
PROJECTNAME=cargo

OWNER=rad
GROUP=users

INSTALL_DJANGO_DIR=/home/rad/django
INSTALL_APACHE_DIR=/var/www-$(PROJECTNAME)

REGEXP_DEVEL_URL=http:\/\/cargo

PO=$(wildcard ./locale/ru/LC_MESSAGES/*.po)
MO=$(patsubst %.po,%.mo,$(PO))

